version: '3.8'

services:
  express:
    build: ./express_app
    container_name: express_app
    ports:
      - "3001:3000"
    environment:
      - MONGO_URI=mongodb://mongo:27017/express_db
    depends_on:
      - mongo

  react:
    build: ./react_app
    container_name: react_app
    ports:
      - "3000:3000"
    stdin_open: true
    tty: true

  mongo:
    image: mongo:6.0
    container_name: mongo_db
    ports:
      - "27017:27017"
    volumes:
      - ./mongo_db/data:/data/db
    environment:
      - MONGO_INITDB_ROOT_USERNAME=root
      - MONGO_INITDB_ROOT_PASSWORD=secret
